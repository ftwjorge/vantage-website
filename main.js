// Initialize scroll animations
AOS.init({
  duration: 600,      // animation duration in ms
  easing: 'ease-out', // easing function
  once: true,         // only animate once per element
  offset: 60          // px from bottom of viewport to trigger
});
